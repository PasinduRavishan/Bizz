{
  "require": ["dotenv/config"],
  "spec": "test/**/*.test.js",
  "timeout": 300000,
  "bail": false,
  "color": true,
  "reporter": "spec",
  "ui": "bdd",
  "node-option": [
    "loader=./loader.mjs"
  ]
}
